# Z-Image-Turbo Web Interface

Современный веб-интерфейс для генерации изображений с использованием модели Z-Image-Turbo от Tongyi-MAI.

## Возможности

- 🎨 Интуитивный веб-интерфейс для генерации изображений
- ⚡ Быстрая генерация на GPU (CUDA)
- 🎯 Регулируемые параметры (размер, количество шагов, seed)
- 💾 Скачивание сгенерированных изображений
- 🔄 Асинхронная генерация с отслеживанием прогресса
- 📱 Адаптивный дизайн (работает на мобильных устройствах)

## Требования

- Python 3.8+
- NVIDIA GPU с поддержкой CUDA (рекомендуется)
- 16GB+ видеопамяти для оптимальной работы

## Установка

### 1. Клонирование/подготовка проекта

```bash
git clone https://github.com/lxstorexxd/Image-Turbo.git
cd Image-Turbo
```

### 2. Создание виртуального окружения (опционально)

```bash
# На Windows
python -m venv venv
venv\Scripts\activate

# На Linux/Mac
python -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

## Изменить путь кэша 
Если хотите сохранить в другое место, установите переменную окружения:
```
set HF_HOME="D:\Project\Image-Turbo\.cache"
```

## Запуск

```bash
python app.py
```

Приложение будет доступно по адресу: **http://localhost:5000**

## Использование

1. Откройте браузер и перейдите на `http://localhost:5000`
2. Введите описание изображения в поле "Image Prompt"
3. (Опционально) Измените параметры:
   - **Resolution**: выбор размера (128x128 или 256x256)
   - **Inference Steps**: количество шагов (больше = лучше качество)
   - **Random Seed**: для воспроизводимости результатов
4. Нажмите кнопку "Generate Image"
5. Дождитесь завершения генерации
6. Нажмите "Download Image" для сохранения

## Параметры

- **Prompt**: Текстовое описание желаемого изображения
- **Resolution**:
  - 128x128: быстро, для тестирования
  - 256x256: более детальные изображения
- **Inference Steps**: 1-20 (рекомендуется 6-12)
  - Меньше = быстрее
  - Больше = лучше качество
- **Random Seed**: 0-2147483647
  - Одинаковый seed = одинаковый результат

## Архитектура

```
Image-Turbo/
├── app.py                 # Flask приложение
├── requirements.txt      # Зависимости
├── templates/
│   └── index.html       # HTML интерфейс
└── static/
    ├── css/
    │   └── style.css    # Стили
    └── js/
        └── script.js    # Клиентский JavaScript
```

## API Endpoints

### POST /api/generate

Запуск генерации изображения

**Request body:**

```json
{
  "prompt": "Описание изображения",
  "height": 128,
  "width": 128,
  "num_steps": 6,
  "seed": 42
}
```

**Response:**

```json
{
  "session_id": "uuid-string"
}
```

### GET /api/status/<session_id>

Проверка статуса генерации

**Response:**

```json
{
  "status": "generating|completed|error",
  "image": "base64-encoded-image",
  "progress": 0-100,
  "error": "error message"
}
```

### GET /api/health

Проверка статуса приложения

## Оптимизация

### Ускорение генерации

Раскомментируйте в `app.py` для включения Flash Attention:

```python
# pipe.transformer.set_attention_backend("flash")    # Flash-Attention-2
# pipe.transformer.set_attention_backend("_flash_3") # Flash-Attention-3
```

### Компиляция модели

```python
# pipe.transformer.compile()  # Компилирует модель DiT для ускорения
```

### CPU Offloading

Для устройств с ограниченной видеопамятью:

```python
# pipe.enable_model_cpu_offload()
```

## Ссылки

- [Hugging Face Model Card](https://huggingface.co/Tongyi-MAI/Z-Image-Turbo)
- [Diffusers Documentation](https://huggingface.co/docs/diffusers)




